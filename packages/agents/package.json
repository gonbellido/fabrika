{
  "name": "@fabrika/agents",
  "version": "1.0.0",
  "private": true,
  "description": "Fabrika multi-agent factory — agent types, orchestrator, message bus",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check .",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}

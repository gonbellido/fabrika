{
  "name": "@fabrika/adapters",
  "version": "1.0.0",
  "private": true,
  "description": "Fabrika adapters — Provider implementations for WooCommerce, Shopify, PostgreSQL",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "prettier --check .",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fabrika/capabilities": "workspace:*",
    "@fabrika/dsl": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
